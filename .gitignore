# Secrets — never commit
.env
data-plane/certs/

# Terraform
.terraform/
# terraform.tfstate is committed for CI state sync
*.tfstate.backup
# .terraform.lock.hcl is committed to lock provider versions
terraform.tfvars

# Generated Kong configs (built in CI)
gateway-config/kong.yaml
gateway-config/final-kong.yaml

# OS files
.DS_Store
*.log
